apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: team2-rabbitmq-dev
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://charts.bitnami.com/bitnami
    chart: rabbitmq
    targetRevision: "*"
    helm:
      values: |
        fullnameOverride: rabbitmq-service
        architecture: standalone
        replicaCount: 1

        # 🎯 [오류 수정]: Bitnami 차트 버그를 우회하기 위해 검증된 이미지 태그를 강제로 지정합니다.
        image:
          tag: 4.0.8-debian-12-r0

        auth:
          username: talkking
          existingPasswordSecret: rabbitmq-secret
          existingSecretPasswordKey: rabbitmq-password

        persistence:
          enabled: false

        service:
          type: ClusterIP

        metrics:
          enabled: true
          serviceMonitor:
            enabled: true
            namespace: monitoring
  destination:
    server: https://kubernetes.default.svc
    namespace: talkking-dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true